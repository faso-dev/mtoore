{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} DASHBOARD
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-header">
                    <h1>Tutoriles</h1>
                </div>
                <div class="card-body">
                    <h1 style="font-weight: 900; font-size: 60px; letter-spacing: 1px; text-align: center">
                        {{ tutoriels }}
                    </h1>
                </div>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="card">
                <div class="card-header">
                    <h1>Catégories</h1>
                </div>
                <div class="card-body">
                   <h1 style="font-weight: 900; font-size: 60px; letter-spacing: 1px; text-align: center">
                       {{ categories }}
                   </h1>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
