{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} Edition du totoriel {{ tutorial.title }}
{% endblock %}

{% block body %}
    <div class="col">
        <div class="card">
            <div class="card-header bg-dark text-white text-center p-4">
                <div class="float-left">
                    <a class="btn btn-dark barka-bg" href="{{ path('tutorial_index') }}"><i class="fa fa-arrow-left"></i> Retour</a>
                </div>
                <h3>Edition du totoriel #{{ tutorial.title }}</h3>
            </div>
            <div class="card-body">
                {{ include('tutorial/_form.html.twig', {'button_label': 'Editer'}) }}            </div>
        </div>
    </div>
{% endblock %}
