{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} Edition of tutorial {{ tutorial.title }}
{% endblock %}

{% block body %}
    <div class="col">
        <div class="card">
            <div class="card-header bg-dark text-white text-center p-4">
                <div class="float-left">
                    <a class="btn btn-dark barka-bg" href="{{ path('tutorial_index') }}"><i class="fa fa-arrow-left"></i> Back</a>
                </div>
                <h3>Edition of #{{ tutorial.title }}</h3>
            </div>
            <div class="card-body">
                {{ include('tutorial/_form.html.twig', {'button_label': 'Edit'}) }}            </div>
        </div>
    </div>
{% endblock %}
