{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} Mise en place du texte de bienvenue
{% endblock %}

{% block body %}
    <div class="col">
        <div class="card">
            <div class="card-header bg-dark text-white text-center p-4">
                <div class="float-left">
                    <a class="btn btn-dark" href="{{ path('category_index') }}"><i class="fa fa-arrow-left"></i> Retour</a>
                </div>
            </div>
            <div class="card-body">
                {{ include('get_started/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
