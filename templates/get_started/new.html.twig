{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} Bot welcome message management
{% endblock %}

{% block body %}
    <div class="col">
        <div class="card">
            <div class="card-header bg-dark text-white text-center p-4">
                <div class="float-left">
                    <a class="btn btn-dark" href="{{ path('category_index') }}"><i class="fa fa-arrow-left"></i> Back</a>
                </div>
            </div>
            <div class="card-body">
                {{ include('get_started/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
