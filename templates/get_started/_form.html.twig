{{ form_start(form) }}
    <div class="row">
        <div class="col-lg-12">
            {{ form_row(form.welcome) }}
        </div>
        <div class="col-lg-12 mt-3">
            <button class="btn btn-dark">{{ button_label|default('Enregistrer') }}</button>
        </div>
    </div>
{{ form_end(form) }}
